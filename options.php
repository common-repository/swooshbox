<?php

// Coming soon to this plugin... an options page!

// Blur and fade background when image pops up?
// CSS selector of elements to apply fade effect to? e.g. <code>.site</code> or <code>#page</code> (default: <code></code>)
// CSS selector of links to which to apply the effect?
// Add borders and shadows to images with links (and make them float when hovered)?
// Use on mobile?
// Tilt images when they pop up?
// Scope of galleries, i.e. the CSS selector for the element within which images will loop using the left/right buttons (default: <code>article</code> with a fallback to <code>body</code> if no articles exist.)